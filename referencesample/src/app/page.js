import Home from "../components/Home";

/**
 * Route: /
 * Order in the UI: Navbar (root layout) → this page → Footer (root layout).
 * Add more sections here as separate components when you grow the homepage.
 */
export default function HomePage() {
  return (
    <>
      <Home />
    </>
  );
}
